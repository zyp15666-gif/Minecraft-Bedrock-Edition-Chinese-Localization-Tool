# 依赖声明文件 - 供 pip-compile 使用
# ================================================
# 生成锁定文件：pip-compile requirements.in -o requirements.lock
# 安装锁定版本：pip-sync requirements.lock
# ================================================
# 本文件与 pyproject.toml [project.dependencies] 保持同步

# 核心依赖
flet>=0.84.0
pyyaml>=6.0.1
requests>=2.32.3
tqdm>=4.66.1
esprima>=4.0.1
pynbt>=2.0.0
mcstructure>=0.0.1b6
numpy>=2.0.0
aiohttp>=3.9.0
cachetools>=5.3.0
pydantic>=2.0.0

# 可选依赖（性能优化）
json5>=0.9.17
screeninfo>=0.8.1
pyahocorasick>=2.0.0
jieba>=0.42.1
psutil>=5.9.0
